# S5 — Minimal pod (default SA, no RBAC escalation wiring) — expect weak path / capability gaps
apiVersion: v1
kind: Pod
metadata:
  name: broken-chain
  namespace: fortuna-test
  labels:
    fortuna.io/e2e-scenario: s5-broken-chain
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sleep", "3600"]
