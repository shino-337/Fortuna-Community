apiVersion: apps/v1
kind: Deployment
metadata:
  name: fortuna-dashboard
  namespace: fortuna
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fortuna-dashboard
  template:
    metadata:
      labels:
        app: fortuna-dashboard
    spec:
      containers:
      - name: dashboard
        image: ghcr.io/shino-337/fortuna-community/fortuna-dashboard:v1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
        resources:
          requests:
            memory: "64Mi"
            cpu: "10m"
          limits:
            memory: "128Mi"
            cpu: "100m"
      volumes:
      - name: nginx-config
        configMap:
          name: fortuna-dashboard-nginx
---
apiVersion: v1
kind: Service
metadata:
  name: fortuna-dashboard
  namespace: fortuna
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: fortuna-dashboard
