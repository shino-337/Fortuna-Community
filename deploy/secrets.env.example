# Copy to deploy/secrets.env and fill in values. deploy/secrets.env is gitignored.
# Used by: scripts/utils/ensure-fortuna-secrets.sh (deploy/rebuild)
#
# NVD API key (optional): higher rate limit for CVE fallback. Get key at https://nvd.nist.gov/developers/request-an-api-key
export NVD_API_KEY=""
