/**
 * Default UI window for runtime_signals list APIs (minutes).
 * Align with core default FORTUNA_RUNTIME_RISK_LOOKBACK_HOURS=24 and GetPodRiskReport summary.runtimeSignals24h.
 */
export const RUNTIME_SIGNALS_LOOKBACK_MINUTES = 24 * 60;
