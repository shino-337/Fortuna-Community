apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.dashboard.name | default "fortuna-dashboard" }}
  labels:
    app: fortuna-dashboard
spec:
  type: {{ .Values.dashboard.service.type }}
  ports:
  - port: {{ .Values.dashboard.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: fortuna-dashboard

