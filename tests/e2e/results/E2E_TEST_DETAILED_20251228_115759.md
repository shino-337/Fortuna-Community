# End-to-End Test Detailed Report

**Date**: $(date)  
**Test Pod**: ${TEST_POD_NAME:-N/A}  
**Pod UID**: ${POD_UID:-N/A}

---

## Test Execution Summary

### 1. Pod Creation ✅
- **Pod Name**: ${TEST_POD_NAME:-N/A}
- **Image**: nginx:1.25-alpine
- **Namespace**: fortuna
- **Creation Time**: $(kubectl get pod -n fortuna $TEST_POD_NAME -o jsonpath='{.metadata.creationTimestamp}' 2>/dev/null || echo "N/A")
- **Status**: Running

### 2. Agent Processing Flow

#### 2.1 Pod Detection ✅
$(kubectl logs -n fortuna $AGENT_POD 2>&1 | grep -E "test-pod-e2e-1766897554.*added|test-pod-e2e-1766897554.*Running" | sed 's/^/    - /' | head -5)

#### 2.2 SBOM Queue Status
- **Queue Workers**: 3 workers active
- **Test Pod Status**: Queued for async processing
- **Current Processing**: Other pods in queue (etcd, coredns, etc.)

#### 2.3 SBOM Extraction
- **Status**: ⏳ Pending (in queue)
- **Note**: SBOM extraction takes 1-5 minutes per pod. Test pod is queued behind other pods.

### 3. Core Processing

#### 3.1 SBOM Reception
- **Status**: ⏳ Waiting for SBOM from Agent
- **Note**: Core is ready and waiting for SBOM data

#### 3.2 CVE Matching
- **Status**: ⏳ Waiting for SBOM
- **Note**: Will start automatically when SBOM is received

#### 3.3 Insight Generation
- **Status**: ⏳ Waiting for CVE matches
- **Note**: Will be created after CVE matching completes

### 4. Database Verification

#### 4.1 SBOM Table
$(kubectl exec -n fortuna $POSTGRES_POD -- psql -U postgres -d ksam -t -c "SELECT COUNT(*) as total_sboms FROM sboms WHERE pod_uid = '$POD_UID' OR pod_name = '$TEST_POD_NAME';" 2>&1 | sed 's/^/    - Total SBOMs for test pod: /')

#### 4.2 Insights Table
$(kubectl exec -n fortuna $POSTGRES_POD -- psql -U postgres -d ksam -t -c "SELECT COUNT(*) as total_insights FROM insights WHERE resource_uid = '$POD_UID';" 2>&1 | sed 's/^/    - Total insights for test pod: /')

### 5. API Verification

- **Endpoint**: /api/v1/insights?resource_uid=${POD_UID:-N/A}
- **Status**: ✅ API accessible
- **Response**: Empty (no insights yet - waiting for SBOM processing)

---

## Processing Timeline

1. **04:52:34** - Pod created
2. **04:52:45** - Pod transitioned to Running
3. **04:52:45** - Agent queued pod for SBOM processing
4. **⏳ Current** - SBOM extraction in queue (waiting for worker availability)

---

## Observations

1. ✅ Agent successfully detected pod
2. ✅ Pod queued for async SBOM processing
3. ⏳ SBOM extraction pending (queue backlog)
4. ✅ Core ready to process SBOM
5. ✅ API endpoint accessible

---

## Queue Status

The SBOM work queue has 3 workers processing pods in parallel. The test pod is queued and will be processed when a worker becomes available. Current queue includes:
- etcd-minikube (in progress)
- coredns (in progress)
- Other system pods

---

## Expected Next Steps

1. Worker becomes available
2. SBOM extraction starts for test pod
3. SBOM sent to Core
4. CVE matching triggered
5. Insights generated
6. Data available in database and API

---

**Report Generated**: $(date)
